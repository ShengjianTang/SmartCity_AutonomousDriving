# 控制板接口边界

当前状态：`BLOCKED`。Host 没有可驱动车辆的控制板实现。

已知的 2022–2023 CarDo/智控板教程提到 GD32F103C8T6、Keil 5/J-Link、72 MHz、USB/UART、舵机/电机/蜂鸣器等能力；这些信息不能自动证明当前 2026 赛具使用同一硬件/固件。

正式接口必须补齐：当前板卡/固件版本、物理端口和电平、端口枚举、波特率与单位、完整帧长定义、端序/浮点表示、地址表、校验覆盖范围、ACK/心跳/超时、复位/断连行为，以及 MCU 源码或抓包验证。此前 `OfficialSerialTransport` 保持不存在。
