# 下一阶段需要用户补充的内容

当前 Host 离线闭环已经可构建、测试和读取真实视频，但完整比赛功能仍不能安全补齐。请按优先级提供以下内容；每批资料放入指定目录后再让我继续，我会先做哈希/许可证/版本审计，再实现相应边界。

## P0：先补齐资料审计缺口

1. 从石墨原页面手动下载“车模组装视频教程”，放到 `official/organizer_drop/hardware/assembly/`。请保留原文件名，不要二次转码。
2. 从石墨原页面下载 `AIC2025学习手册.pdf`，放到 `official/organizer_drop/notices/`。当前自动访问只得到防盗链占位图，没有绕过。
3. 如能下载，补 `场地铺设说明20250922.pdf` 与 2025 技术分享 PDF，分别放到 `maps/`、`notices/`。它们仍是旧资料，但可完善审计。

## P1：决定能否开始目标板/控制链路实现

1. 当前 2026 赛具清单或设备铭牌照片：计算单元、相机、控制板、舵机、电机、编码器、电池。
2. 目标板开机后的原始输出：`uname -a`、`cat /etc/os-release`、CPU/内存、USB/串口/视频设备枚举；放 `docs/environment/target_evidence/`。
3. 与当前固件完全匹配的 MCU 完整源码工程、编译说明、固件版本和接线图；放 `mcu/official/`。
4. 版本化串口协议原文；最好同时提供 MCU 源码对应解析函数和三类抓包：心跳、零速/停止、正常转向。放 `official/organizer_drop/sdk/control/`。
5. 物理急停、看门狗、断开连接和 Host 崩溃时电机行为的官方说明。

没有这一批，我不能实现或启用 `OfficialCameraBackend`、`OfficialInferenceBackend`、`OfficialSerialTransport`，也不能声称实车安全停止。

## P2：决定能否实现感知、规划和控制

1. 2026 最终地图、详细尺寸、元素摆放规则、最终标牌/道闸/人偶/锥桶实物样式与最终类别定义。
2. 合法训练集/验证集，或允许团队采集的书面规则；同时提供标注规范和数据授权。
3. 官方模型或团队训练权重的许可证、训练配置、数据版本、类别顺序、输入输出、转换日志。
4. 当前赛具真实车载原始视频，覆盖直道、弯道和每类任务元素；保留原始帧率/分辨率/时间信息。
5. 相机内外参、车辆轴距/轮距/轮胎、舵机中值/限位/角度映射、电机/编码器/制动距离/转弯半径/停车轨迹的原始测量记录。

## P3：实现后才能补的验证材料

- 目标板 SDK/算子/转换器复现记录与板端基准。
- 封闭场地分阶段测试：架空轮、低速直线、转向、停止链路、单任务、完整两圈。
- 固定验证集模型精度、板端时延、系统端到端时延和失败案例。
- 最终赛区通知、提交窗口、比赛视频和裁判记录。

## 文件验收要求

请尽量保留原始文件、来源 URL/发送人、获取时间和版本号；不要只给截图替代可解析文档，不要把未知参数整理成经验值。若资料含账号、Token、私人信息或超大原始数据，先说明位置，不要提交到 Git。
