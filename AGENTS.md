# AGENTS.md

本文件约束本目录及所有子目录中的后续 Codex/代理工作。

## 项目性质

本项目是证据驱动的真实车辆竞赛工程。官方文件、官方 SDK/示例、真实硬件手册、留档标定和真实场地测试依次优先；第三方项目和一般经验只能参考。

## 修改前检查

1. 修改需求、架构、接口、配置或控制逻辑前，先阅读：
   - `docs/requirements/requirement_traceability_matrix.md`
   - `docs/sources/fact_registry.yaml`
   - `docs/requirements/blockers.md`
   - `PROJECT_STATE.md`
2. 不得为了让任务看似完成而删除、弱化或绕过阻塞项。
3. 新外部资料必须先记录 URL、SHA-256 或完整 commit SHA，再用于结论。

## 禁止推测与伪实现

1. 禁止推测具体计算单元、OS、SDK、摄像头、MCU、电机、舵机、编码器、设备节点、串口参数/帧、车辆几何、模型、数据、赛道布局或控制参数。
2. 不得自行认定规则规格对应某个具体计算单元型号。
3. 不得自行引入规则未允许的传感器。
4. 未知配置保持 `null`，并填写 `source_required`；不得填经验值。
5. 所有魔法数字必须来自官方文件或可追溯标定文件，并登记来源。
6. 禁止用随机数据、固定检测框、固定成功返回、空权重、假设备或延时动作冒充功能。
7. 暂不可用的后端必须明确返回“不支持/未配置”错误并阻止相关功能，不得静默成功。

## 第三方与许可证

1. 所有第三方仓库只放在 `third_party/`，只能作为 `REFERENCE_ONLY`。
2. 不得直接修改第三方工作树。
3. 未确认许可证前不得复制第三方源码、模型或二进制到正式工程。
4. 当前 `IntelligentCar_Baidu` 的整体许可证为 `UNVERIFIED`；其参数、硬件和协议不是本赛题事实。

## 状态与验证

1. 只使用 `VERIFIED`、`IMPLEMENTED_UNVERIFIED`、`BLOCKED`、`NOT_STARTED`、`REJECTED`、`REFERENCE_ONLY`。
2. 未经真实目标硬件或真实赛道测试，不得把车辆功能标为 `VERIFIED`。
3. 编译通过只证明 Host Core 能在该主机编译，不证明实车功能。
4. 新功能必须补充自动化测试和对应验证记录；测试夹具不得称为真实传感器数据。
5. 任何性能、精度、速度、成功率或用时结论必须引用真实测量记录。
6. 每次实质变更同步更新 `PROJECT_STATE.md` 和 `CHANGELOG.md`。

## 安全与数据

1. 配置缺失、后端缺失、输入过期、状态非法或通信异常时采取显式失败/安全停止语义。
2. 在真实急停接口验证前，只能设计接口，不得声称已实现实车急停。
3. 不提交密钥、账号、Token、私人资料、大体积原始数据、设备视频、训练缓存或中间模型。
4. 日志不得包含密钥；新参数和资料必须保留来源与时间。
